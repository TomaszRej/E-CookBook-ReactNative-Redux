
const initialState = {
    userName: 'Tom-z- ReduxState',
    users: [],
    // selectedPlace: null
};

const reducer = (state=initialState, action) => {
        return state;
};

export default reducer;
